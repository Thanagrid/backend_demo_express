# 📂 Guidelines
เอกสารฉบับนี้อธิบายวิธีการใช้งานเพื่อให้ทีมพัฒนาสามารถใช้ project นี้ในการทดลองยิง API ที่เกี่ยวข้องกับการ Login จาก Frontend ได้อย่างถูกต้อง

---
## ⚙️ การตั้งค่าก่อนการใช้งาน

### 🟢 ENV
- เมื่อ Clone Project ลงเครื่องแล้ว ให้เพิ่มไฟล์ .env โดยดูตัวอย่าง key ต่างๆ ที่จำเป็นต้องใช้ใน .env.example
- **อธิบาย KEY ใน .env**
  - `JWT_SECRET_KEY` : เก็บ key ที่ใช้สำหรับเข้ารหัส JWT Token ในขั้นตอนทำ user authen

### 🟢 index.js

- **แก้ไขการตั้งการการเชื่อมต่อ DB**
ต้องแก้ไขรายละเอียดตรงนี้ให้ถูกต้องก่อนเพื่อเชื่อมต่อ DB ที่ถูกต้อง

```
const db = new Pool({
  user: '{ชื่อ user postges ให้เครื่องของท่าน}',
  host: 'localhost',
  database: '{ชื่อ DB ที่ต้องการเชื่อมต่อ}',
  password: '{password ให้เครื่องของท่าน}',
  port: 5432,
})

```
> แก้ไขในส่วนที่อยู่ใน {}

หากการเชื่อมต่อถูกต้องเมื่อรัน `npm run dev` แล้ว จะได้ผลลัพธ์ดังนี้
```
  ✅ Database connected successfully
  🚀 Server running at http://x.x.x.x:3030
```